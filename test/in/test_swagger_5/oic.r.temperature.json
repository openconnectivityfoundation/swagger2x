{
  "properties": {
    "temperature": {
      "description": "Current temperature setting or measurement",
      "type": "number"
    },
    "units": {
      "description": "Units for the temperature value",
      "enum": [
        "C",
        "F",
        "K"
      ],
      "readOnly": true
    }
  },
  "required": [
    "temperature"
  ]
}